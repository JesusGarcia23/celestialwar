import { Angel, Demon } from '../Character';

export const generatePlayer = (canvas, listOfPlayers) => {
    console.log(listOfPlayers);
}

export const playerCreator = (canvas, listOfPlayers) => {
    
    return generatePlayer()
}